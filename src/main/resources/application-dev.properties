# OAuth2
spring.security.oauth2.resourceserver.jwt.issuer-uri=https://dev-xmd0t7c7kpg2tvxf.us.auth0.com/
spring.security.oauth2.resourceserver.jwt.audience=https://api.dev-collectables.com

# Connection pool settings
spring.datasource.hikari.maximum-pool-size=5
spring.datasource.hikari.minimum-idle=1

# Logging SQL statements (optional, for development)
spring.jpa.show-sql=true

cors.allowed-origins=http://localhost:5173